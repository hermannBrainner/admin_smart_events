import 'package:flutter/material.dart';

const double centDouble = 100;

// Paramètres Cérémonies
const int CEREMONIE_ID_NBRE_DIGITS = 5;

const int BILLET_ID_NBRE_DIGITS = 10;

const int DIVISEUR_GLOBAL = 97;

const Duration delaiMaxAffichage = Duration(hours: 24);

const Duration deltaMinMsge = Duration(minutes: 40);

// Rayons
const Radius rayonMax_Chat = Radius.circular(20);
const Radius rayonMin_Chat = Radius.circular(3);
const Radius rayonZero_Chat = Radius.circular(3);
